[project]
name = "pro-clubs-checkin-bot"
version = "0.1.0"
description = "Pro Clubs Daily Check-In Bot for Discord"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
